name: robolab_mobile
description: RoboLab Android-first engineering workspace client.
publish_to: "none"
version: 4.3.1+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

flutter:
  uses-material-design: true
