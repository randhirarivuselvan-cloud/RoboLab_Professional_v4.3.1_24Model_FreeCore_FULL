{
  "role": "compiler_1",
  "display_name": "Compiler AI #1",
  "purpose": "Independently validate and assemble project artifacts into a consistent compile-ready result.",
  "base_model_env": "ROBO_BASE_MODEL",
  "output_dir": "models/compiler_1",
  "dataset": "training/data/compiler_1.jsonl",
  "lora": {
    "r": 32,
    "alpha": 64,
    "dropout": 0.05,
    "target_modules": [
      "q_proj",
      "k_proj",
      "v_proj",
      "o_proj"
    ]
  },
  "training": {
    "epochs": 3,
    "learning_rate": 0.0002,
    "per_device_train_batch_size": 1,
    "gradient_accumulation_steps": 8,
    "max_seq_length": 4096
  },
  "output_contract": {
    "type": "json",
    "required_fields": [
      "status",
      "analysis",
      "artifacts",
      "risks",
      "next_actions"
    ]
  }
}