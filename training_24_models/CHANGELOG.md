# v4.3.0
- Added a 24-role specialist neural-model training suite.
- Added per-role LoRA configuration and starter datasets.
- Added a 24-role training launcher with configurable worker count.
- Added per-role model path registry for inference routing.
- Added explicit documentation distinguishing training infrastructure from trained weights.
